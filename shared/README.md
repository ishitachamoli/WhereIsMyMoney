# Shared Types

This directory contains shared type definitions between the frontend and backend.
Currently, the API contract is defined by the backend's Pydantic schemas.
The frontend consumes these via API calls.
